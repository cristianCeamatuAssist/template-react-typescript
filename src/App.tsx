import { BrowserRouter } from 'react-router-dom'
// pages
import { Router } from 'pages'

const App = () => {
  return (
    <BrowserRouter>
      <Router />
    </BrowserRouter>
  )
}

export default App
